import { signOut } from "firebase/auth";
import React from "react";
import { authService } from "../fbase";

const Home = () => {
  return (
    <>
      <span>Home</span>
    </>
  );
};

export default Home;
